<UserControl xmlns="https://github.com/avaloniaui"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:vm="clr-namespace:HamiltonianPath.Avalonia.ViewModels"
             x:Class="HamiltonianPath.Avalonia.Views.Controls.HamiltonianBoardView">
  <Border Classes="board-surface">
    <ItemsControl ItemsSource="{Binding Cells}">
      <ItemsControl.ItemsPanel>
        <ItemsPanelTemplate>
          <UniformGrid Rows="{Binding $parent[ItemsControl].DataContext.BoardHeight}" Columns="{Binding $parent[ItemsControl].DataContext.BoardWidth}" />
        </ItemsPanelTemplate>
      </ItemsControl.ItemsPanel>
      <ItemsControl.ItemTemplate>
        <DataTemplate>
          <Border Background="{Binding BackgroundBrush}"
                  BorderBrush="{Binding BorderBrush}"
                  BorderThickness="1"
                  CornerRadius="10"
                  Margin="4"
                  Tag="{Binding}">
            <Border.Transitions>
              <Transitions>
                <BrushTransition Property="Background" Duration="0:0:0.12" />
                <BrushTransition Property="BorderBrush" Duration="0:0:0.12" />
              </Transitions>
            </Border.Transitions>
            <Grid Background="Transparent"
                  PointerPressed="Cell_PointerPressed"
                  PointerEntered="Cell_PointerEntered"
                  PointerReleased="Cell_PointerReleased"
                  RowDefinitions="*,*"
                  ColumnDefinitions="*,*">
              <Border Grid.Row="0"
                      Grid.ColumnSpan="2"
                      Width="8"
                      CornerRadius="2"
                      Background="#8E86FF"
                      HorizontalAlignment="Center"
                      VerticalAlignment="Stretch"
                      IsVisible="{Binding LinkTop}" />
              <Border Grid.RowSpan="2"
                      Grid.Column="1"
                      Height="8"
                      CornerRadius="2"
                      Background="#8E86FF"
                      HorizontalAlignment="Stretch"
                      VerticalAlignment="Center"
                      IsVisible="{Binding LinkRight}" />
              <Border Grid.Row="1"
                      Grid.ColumnSpan="2"
                      Width="8"
                      CornerRadius="2"
                      Background="#8E86FF"
                      HorizontalAlignment="Center"
                      VerticalAlignment="Stretch"
                      IsVisible="{Binding LinkBottom}" />
              <Border Grid.RowSpan="2"
                      Grid.Column="0"
                      Height="8"
                      CornerRadius="2"
                      Background="#8E86FF"
                      HorizontalAlignment="Stretch"
                      VerticalAlignment="Center"
                      IsVisible="{Binding LinkLeft}" />
              <TextBlock Text="{Binding DisplayText}"
                         Foreground="{Binding ForegroundBrush}"
                         HorizontalAlignment="Center"
                         VerticalAlignment="Center"
                         FontWeight="SemiBold"
                         FontSize="16" />
            </Grid>
          </Border>
        </DataTemplate>
      </ItemsControl.ItemTemplate>
    </ItemsControl>
  </Border>
</UserControl>
